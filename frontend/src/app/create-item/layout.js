export const metadata = {
  title: "Create Item | Tindally",
  description: "Add a new item to your store inventory.",
};

export default function CreateItemLayout({ children }) {
  return children;
}
